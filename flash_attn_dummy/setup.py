from setuptools import setup, find_packages

setup(
    name="flash_attn",
    version="2.0.0",
    packages=find_packages(),
    description="Dummy flash_attn package to satisfy import checks",
)
